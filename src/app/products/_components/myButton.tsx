// "use client";
// export default function MyButton({
//   category,
// }: {
//   category: { id: number; name: string };
// }) {
//   return (
//     <button type="button" key={category.id} onClick={() => test(category.name)}>
//       {category.name}
//     </button>
//   );
// }
